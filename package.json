{
  "name": "undercrypt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "ts-node src/cli.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6",
    "vitest": "^1.0.0",
    "@types/node": "^20.3.1"
  }
}
